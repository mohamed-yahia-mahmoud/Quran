name: quran
description: A new quran Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-417.0.dev <4.0.0'

dependencies:
  cached_network_image: ^3.2.3
  connectivity_plus: ^4.0.1
  cupertino_icons: ^1.0.2
  dio: ^5.1.1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^8.1.2
  freezed_annotation: ^2.2.0
  get_it: ^7.3.0
  injectable: ^2.1.1
  json_annotation: ^4.8.0
  just_audio: ^0.9.32
  path_provider: ^2.0.15
  shared_preferences: ^2.1.0
  uuid: ^3.0.7
  flutter_cache_manager: ^3.3.1
  dartz: ^0.10.1

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.3.2
  injectable_generator: ^2.1.5
  json_serializable: ^6.6.1

flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/home_outline.png
    - assets/images/home_filled.png
    - assets/images/like_outline.png
    - assets/images/like_filled.png
    - assets/images/more_outline.png
    - assets/images/more_filled.png
    - lang/ar.json
    - lang/en.json